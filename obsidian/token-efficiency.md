# Token Efficiency

The graph-guided mode reads the vault index, hot context, graph report, parser, and parser tests. The naive mode reads every source and upstream snapshot file before forming a hypothesis.

See [[../reports/TOKEN_EFFICIENCY.md]] for the measured comparison.
